# Intel physical boards keep the SPI controller writable while Heads runs,
# then lock protected ranges immediately before kexec.
include $(pwd)/boards/starlabs/physical.config

CONFIG_IO386=y
export CONFIG_FINALIZE_PLATFORM_LOCKING=y
